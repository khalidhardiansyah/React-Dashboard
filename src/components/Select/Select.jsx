import React from 'react'

function Select() {
  return (
        <select className={`form-select w-full border-0 focus:ring-0 text-black bg-transparent text-opacity-40 text-xs`}>
            <option value="" className=' text-xs'>This Week</option>
        </select>

  )
}

export default Select